import {StyleSheet} from 'react-native';
import {white} from '../utils/StylesConstants';

export const CommonStyles = StyleSheet.create({
  commonContainer: {
    flex: 1,
    backgroundColor: '#FAF9F6',
  },
});
